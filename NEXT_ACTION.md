# NEXT ACTION

> **File contract** — Purpose: the single most important next step, unambiguous. Owner: every cycle (write at end). Update rules: overwrite. Recovery: this is what a resuming Claude does FIRST after bootstrap. Format: one imperative action + acceptance check. Lifecycle: volatile.

## Do this next

**Context:** This cycle ran net-new discovery (TASK-0016) with a **productization-vacuum screen across 4 sectors first** (applying the ADR-012 learning). Rejected EU CBAM (10+ mature emissions-data vendors) and EU AI Act conformity (crowded AI-governance GRC) at the screen; deep-dived **SEC cyber-incident materiality-determination & disclosure-defense** → **OPP-003 scored 72 → MONITOR** (ADR-013), not promoted: better vacuum than CBAM but still capped by active Item-1.05 rescission risk + forming competition (Kovrr/FAIR-MAM) + episodic frequency. Project 01 remains **VALIDATION HOLD, operator-gated** (TASK-0014). No second ≥80 project. Filed **TASK-0017** (discovery heuristic, LOW).

**Action (autonomous worker):** Leading project still operator-gated, no other ≥80 project → **do NOT idle, do NOT re-desk-validate project 01.** **Continue NET-NEW discovery (runtime §5 / priority 3)** in a **fresh sector** (do NOT re-run IRA, PFAS, CBAM, EU AI Act, or SEC-cyber). Pick a sector and find + score one Blue-Ocean B2B opportunity using `skills/opportunity-research/SKILL.md` + the ADR-008 rubric, with disprove discipline.

**Apply TASK-0017 heuristic (load-bearing this cycle):** 3 consecutive 70s (OPP-001/002/003) vs the one promoted 83 (OPP-001b) show that once a compliance forcing-function is *public*, horizontal SaaS/consultancies close the vacuum in ~1–2 years. So bias selection toward **(a) a regime whose forcing-function JUST landed** (vacuum still open, incumbents haven't pivoted) **or (b) a sharp DEFENSIVE sub-wedge incumbents structurally can't serve** (the OPP-001b "contested post-event defense / record-reconstruction" pattern — a legal/liability artifact, not a reporting form or a quantification dashboard). Screen each candidate for the vacuum BEFORE deep research. If it scores ≥80 → create `projects/02-<slug>/`; else log to the ledger (MONITOR/ARCHIVE).

**Candidate sectors to consider (un-vetted hints, screen for vacuum first):** FDA §524B premarket medical-device cybersecurity (SBOM/threat-model — specialist players exist: MedCrypt/Cybellum/Finite State, screen carefully); DORA ICT third-party register (likely crowded — GRC incumbents); EU Deforestation Regulation (EUDR) geolocation/traceability due-diligence (forcing-function timing shifting — check if just-landed); a *defensive sub-wedge* inside an already-mature regime (e.g., contested-defense/record-reconstruction layer that incumbents' preventive/quantification tools don't cover).

**Operator actions outstanding (do NOT auto-do):**
1. **TASK-0014** — primary discovery for project 01: 5–10 buyer/firm interviews (WTP + compete-vs-channel) + FOIA/TIGTA/§6418 data (real audit cadence). The ONLY thing that unblocks project 01 → BUSINESS_MODEL.md.
2. **TASK-0002** — enable unattended systemd loop (`automation/install.sh`) — operator go-ahead required.
3. Rotate Discord token (historically pasted in chat).

**Acceptance check:** Next cycle produces a new scored opportunity record in `research/` + a row in `research/OPPORTUNITIES.md` + RESEARCH.md (and a `projects/02-*/` if ≥80); state files refreshed; Discord posted; committed. Do not re-validate project 01 (operator-gated) and do not re-score IRA/PFAS/CBAM/EU-AI-Act/SEC-cyber.

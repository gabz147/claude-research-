# NEXT ACTION

> **File contract** — Purpose: the single most important next step, unambiguous. Owner: every cycle (write at end). Update rules: overwrite. Recovery: this is what a resuming Claude does FIRST after bootstrap. Format: one imperative action + acceptance check. Lifecycle: volatile.

## Do this next

**Context:** The most recent discovery cycle (OPP-005, committed `210f44a`) LEFT regulatory-compliance per ADR-014 and picked an operational money-leak — ocean-freight **demurrage & detention (D&D) overcharge dispute & recovery** — then **killed it at the vacuum screen → 64 → ARCHIVE** (both wedges already owned: prevention = Windward/BuyCo/Cargoo/FourKites/Portcast; recovery = nVision/OceanAudit/Cass + a mature contingency freight-audit industry). That cap FALSIFIED "leave compliance was the fix" and produced **ADR-015**: the binding constraint is **VISIBLE/NAMED MONEY POOLS**, not compliance. The cycle immediately before this one was pure **stabilization** — it reconciled the stale pointer files (they'd been left on OPP-004) and finalized the orphaned `notify_discord.sh` embed feature. Project 01 remains **VALIDATION HOLD, operator-gated** (TASK-0014). Still no second ≥80 project.

**Action (autonomous worker):** Leading project still operator-gated, no other ≥80 → **do NOT idle, do NOT re-desk-validate project 01.** **Continue NET-NEW discovery (runtime §5 / priority 3)** on the **SEEDED leading candidate**, applying the **ADR-015 doctrine (load-bearing):**

> **SEEDED candidate: construction delay / change-order claim-defense documentation.** Buyers = contractors / subcontractors / project owners. Shape = contested-defense + record-reconstruction artifact (schedules, RFIs, daily logs, change-order trails) assembled to win/defend a delay or change-order claim. Hypothesis for why it may pass where 5 priors failed: it is **fragmented, embarrassing, un-named-budget pain** (no single headline $-pool stat) currently served only by a **services-model claims-consultant industry** (e.g. delay-analysis experts), with **no productized owner**. **This hypothesis is UNVERIFIED — run the vacuum check first.**

ADR-015 discovery rules (apply in order, BEFORE any deep research):
1. **Universal named-incumbent vacuum check FIRST, both sides.** Name the actual incumbents on BOTH the *prevention/visibility* side (scheduling/PM software: Primavera P6, Procore, schedule-analytics, Track-record / forensic-schedule tools) AND the *contested-recovery/defense* side (claims consultants, delay-analysis firms, construction-claim software). Show the **specific artifact none of them sell**. Do NOT assume "services-only, no product" — verify it affirmatively (OPP-003/OPP-004/OPP-005 all proved the obvious wedge was already claimed).
2. **De-prioritize / be suspicious of any candidate with a headline $-stat** (a visible/named money pool has already recruited incumbents on both prevention and recovery).
3. **Hunt fragmented / embarrassing / un-named-budget pain** with a clear high-ticket B2B buyer and no dominant incumbent. Target shape = the OPP-001b 83 shape (contested post-event defense + record reconstruction, un-productized).

Find + score ONE Blue-Ocean B2B opportunity using `skills/opportunity-research/SKILL.md` + the ADR-008 rubric, with disprove discipline. **Vacuum-screen the candidate BEFORE deep research.** If ≥80 → create `projects/02-<slug>/` + validation artifacts; else log to the ledger (MONITOR/ARCHIVE) and seed the next candidate.

**Do NOT re-run:** IRA, PFAS, CBAM, EU AI Act, SEC-cyber, EU Pay Transparency, **ocean-freight D&D** (all scored). Do NOT re-desk-validate project 01 (operator-gated).

**Operator actions outstanding (do NOT auto-do):**
1. **TASK-0014** — primary discovery for project 01: 5–10 buyer/firm interviews (WTP + compete-vs-channel) + FOIA/TIGTA/§6418 data (real audit cadence). The ONLY thing that unblocks project 01 → BUSINESS_MODEL.md.
2. **TASK-0002** — enable unattended systemd loop (`automation/install.sh`) — operator go-ahead required.
3. Rotate Discord token (historically pasted in chat).

**Acceptance check:** Next cycle produces a new scored opportunity record in `research/` + a row in `research/OPPORTUNITIES.md` + RESEARCH.md (and a `projects/02-*/` if ≥80); state files refreshed; Discord posted; committed. The construction-claims candidate must pass (or fail) an **affirmative named-incumbent vacuum check on BOTH sides** before any deep research. Do not re-validate project 01 and do not re-score any already-scored sector.

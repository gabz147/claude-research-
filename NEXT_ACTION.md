# NEXT ACTION

> **File contract** — Purpose: the single most important next step, unambiguous. Owner: every cycle (write at end). Update rules: overwrite. Recovery: this is what a resuming Claude does FIRST after bootstrap. Format: one imperative action + acceptance check. Lifecycle: volatile.

## Do this next

**Context:** The most recent discovery cycle (TASK-0024) ran the org's **first deliberately OWNED-POSITION candidate** — a **specialty-insurance MGA/program for emerging hard-to-place lithium-ion / BESS commercial fire risk** (an MGA owns *distribution* + sits in the *underwriting transaction* + accumulates *proprietary loss data*; **AI = the underwriting tool, not the product**). It was built specifically to pass the ADR-018 strip-out-the-AI screen — and **it PASSED that screen** (first candidate to do so; strip out the AI and an MGA is still a real business — NOT the documentation archetype). **But it still capped at the second-stage vacuum check → 58 → ARCHIVE** (fifth Archived row; **8th consecutive cap**, 79/71/72/63/64/58/54/58). Three gates sank it: (1) placement wedge owned — **NARDAC (Amwins MGA)** secured BESS binding authority Mar-2024, **$50M from 8 Lloyd's syndicates**, + Amwins BESS line + Travelers + BikeInsure (micromobility); (2) the "vacuum" is a **transient hard-market cycle** softening into 2026 (capacity returning), not structural; (3) the moat fails twice — AI underwriting is a **commoditized purchasable platform** (hyperexponential/Earnix/Federato), and the loss-data moat is a **cold-start** that accrues only to incumbents — plus an MGA needs **regulated binding authority/capital** (un-buildable by a small org). → **ADR-019.** Project 01 remains **VALIDATION HOLD, operator-gated** (TASK-0014). Still no second ≥80 project.

**Action (autonomous worker):** Leading project still operator-gated, no other ≥80 → **do NOT idle, do NOT re-desk-validate project 01.** **Continue NET-NEW discovery (runtime §5 / priority 3)**, applying the **DOCTRINE STACK in sequence, BEFORE deep research:**

1. **(ADR-018 — lead screen, still in force) Strip-out-the-AI screen.** "If I strip out the AI, is there still a business — a workflow I own, a transaction I sit in, data only I have, or a channel only I reach?" If the product IS the AI reading/comprehending documents, **REJECT** before spending a cycle. (This screen *works* — OPP-008 correctly passed it; the 7 documentation-archetype picks correctly fail it.)
2. **(ADR-015 — second stage) Both-sides named-incumbent vacuum check.** Name the capture/prevention-side AND the recovery/comprehension-side incumbents; show the specific artifact/position none of them sell; verify "no owner" **affirmatively** before any deep research.
3. **(ADR-019 — NEW third stage; OPP-008 passed (1) and still capped, so this gate is load-bearing) Owned-position filter — all three sub-tests must pass:**
   - **(a) Durable vacuum** — is the gap *structural*, or a transient cycle (hard-market, temporary capacity withdrawal, a one-off regulatory deadline) that incumbents refill in 12–24 months? Reject cyclical "vacuums."
   - **(b) Zero-state moat** — can YOU originate the moat from nothing (a network effect / workflow lock-in that compounds from the FIRST customers), or does it only accrue to whoever is already the incumbent (**cold-start data trap**)? Reject moats you can only build by first being the incumbent.
   - **(c) Buildable without heavy regulated capital** — can a small team stand it up, or does it need a charter / licensing / underwriting capacity / balance sheet? Reject regulated-capital businesses (MGA/lender/insurer) unless they uniquely clear this gate.

**Implied next-candidate profile:** an owned-position play whose moat is **originable from zero** (a two-sided workflow/network that compounds with the first customers), in a **structurally** (not cyclically) unserved niche, **buildable without a regulated charter or carrier capacity.**

Find + score ONE Blue-Ocean B2B opportunity using `skills/opportunity-research/SKILL.md` + the ADR-008 rubric, with disprove discipline. **Run the three screens in order BEFORE deep research.** If ≥80 → create `projects/02-<slug>/` + validation artifacts; else log to the ledger (MONITOR/ARCHIVE) and record the meta-learning.

**Do NOT re-run:** IRA, PFAS, CBAM, EU AI Act, SEC-cyber, EU Pay Transparency, ocean-freight D&D, construction delay/change-order claims, legacy PLC/SCADA/DCS comprehension, **Li-ion/BESS specialty insurance** (all scored). Do NOT pick another documentation/reconstruction/comprehension artifact (archetype failure). Do NOT pick a regulated-capital business unless it uniquely clears ADR-019 gate (c). Do NOT re-desk-validate project 01 (operator-gated).

**Operator actions outstanding (do NOT auto-do):**
1. **TASK-0014** — primary discovery for project 01: 5–10 buyer/firm interviews (WTP + compete-vs-channel) + FOIA/TIGTA/§6418 data (real audit cadence). The ONLY thing that unblocks project 01 → BUSINESS_MODEL.md.
2. **TASK-0002** — enable unattended systemd loop (`automation/install.sh`) — operator go-ahead required.
3. **TASK-0021** — cycles must run as `tar` (not `sudo`) for git/file writes, or the supervisor must `chown` the repo to `tar` on start, to stop intermittent `.git`/queue permission failures.
4. Rotate Discord token (historically pasted in chat).

**Acceptance check:** Next cycle produces a new scored opportunity record in `research/` + a row in `research/OPPORTUNITIES.md` + RESEARCH.md (and a `projects/02-*/` if ≥80); state files refreshed; Discord posted; committed. The candidate must **pass all three screens in order** — ADR-018 strip-out-the-AI (own a workflow/transaction/data/channel, not a documentation artifact), THEN the both-sides vacuum check, THEN the ADR-019 owned-position filter (durable vacuum + zero-state moat + buildable without regulated capital). Do not re-validate project 01 and do not re-score any already-scored sector.

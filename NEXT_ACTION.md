# NEXT ACTION

> **File contract** — Purpose: the single most important next step, unambiguous. Owner: every cycle (write at end). Update rules: overwrite. Recovery: this is what a resuming Claude does FIRST after bootstrap. Format: one imperative action + acceptance check. Lifecycle: volatile.

## Do this next

**Context:** The most recent discovery cycle ran the SEEDED candidate (TASK-0022) — **construction delay / change-order claim-defense documentation** — and **killed it at the both-sides vacuum screen → 58 → ARCHIVE** (third Archived row). Both wedges are owned AND productized: capture/prevention = **Procore** Change-Order software + Groundbreak-2025 **AI Daily-Log + RFI agents** ("litigation-grade" records at capture time) on **Primavera P6**; contested-recovery = **SmartPM** (AI delay-analysis built by a forensic expert, "**court-ready in minutes**," >50% of Top ENR GCs) + EOT-claim products (WeBuild/Flexbase/Opteam) + a mature forensic-claims consultant bench (BRG/VERTEX/Robson/…). This is the **6th consecutive cap (79/71/72/63/64/58)** vs the single 83 (OPP-001b), and the **2nd consecutive non-compliance cap** → **ADR-017**. Project 01 remains **VALIDATION HOLD, operator-gated** (TASK-0014). Still no second ≥80 project.

**Action (autonomous worker):** Leading project still operator-gated, no other ≥80 → **do NOT idle, do NOT re-desk-validate project 01.** **Continue NET-NEW discovery (runtime §5 / priority 3)**, but the candidate is now **DELIBERATELY UN-SEEDED** — the seeding heuristic itself is the demonstrated failure mode (it has produced 6 caps). Apply the **ADR-017 doctrine (load-bearing), in order, BEFORE deep research:**

1. **STOP seeding candidates with a court-enforceable dispute right OR a headline $-stat.** ADR-017's core finding: such pains are **DOUBLE incumbent-magnets** — the visible money pool funds *both* a prevention-SaaS category AND a forensic/contingency services industry, so both the prevention and the recovery wedge fill. (Construction claims, ocean D&D, PFAS, SEC-cyber, IRA, EU-PTD all fit this and all capped.)
2. **Before seeding ANY candidate, ask: "is there already a profitable *services* industry organized around this pain?"** If yes → assume productization is in-flight and do **NOT** treat it as "no productized owner." "Served only by consultants today" has been a **false** signal of vacuum **four times** (OPP-003/004/005/006) — SmartPM is literally a forensic expert who productized.
3. **Hunt UN-PRICED / embarrassing / orphaned-ownership pain with NO existing services industry and NO headline $-stat** — a high-ticket B2B buyer whose pain nobody has yet named a budget line for, so no incumbent (product OR services) has been magnetized. This is the only profile ADR-015/017 logic does not already predict will cap.
4. **STILL run the universal both-sides named-incumbent vacuum check FIRST** (name prevention-side and recovery/defense-side incumbents; show the specific artifact none sell) before any deep research. Verify "no owner" affirmatively — do not assume it.

Find + score ONE Blue-Ocean B2B opportunity using `skills/opportunity-research/SKILL.md` + the ADR-008 rubric, with disprove discipline. **Vacuum-screen the candidate BEFORE deep research.** If ≥80 → create `projects/02-<slug>/` + validation artifacts; else log to the ledger (MONITOR/ARCHIVE) and record the meta-learning.

**Do NOT re-run:** IRA, PFAS, CBAM, EU AI Act, SEC-cyber, EU Pay Transparency, ocean-freight D&D, **construction delay/change-order claims** (all scored). Do NOT re-desk-validate project 01 (operator-gated). Do NOT seed a new dispute/$-stat candidate (that is the failure mode).

**Operator actions outstanding (do NOT auto-do):**
1. **TASK-0014** — primary discovery for project 01: 5–10 buyer/firm interviews (WTP + compete-vs-channel) + FOIA/TIGTA/§6418 data (real audit cadence). The ONLY thing that unblocks project 01 → BUSINESS_MODEL.md.
2. **TASK-0002** — enable unattended systemd loop (`automation/install.sh`) — operator go-ahead required.
3. **TASK-0021** — cycles must run as `tar` (not `sudo`) for git/file writes, or the supervisor must `chown` the repo to `tar` on start, to stop intermittent `.git` permission failures. (This cycle hit it again on `queue/active` — fixed ad-hoc with `sudo chown -R tar:tar queue/active`.)
4. Rotate Discord token (historically pasted in chat).

**Acceptance check:** Next cycle produces a new scored opportunity record in `research/` + a row in `research/OPPORTUNITIES.md` + RESEARCH.md (and a `projects/02-*/` if ≥80); state files refreshed; Discord posted; committed. The candidate must NOT be a dispute/$-stat play, must pass the "no existing services industry" filter, and must pass an affirmative both-sides named-incumbent vacuum check before any deep research. Do not re-validate project 01 and do not re-score any already-scored sector.

# NEXT ACTION

> **File contract** — Purpose: the single most important next step, unambiguous. Owner: every cycle (write at end). Update rules: overwrite. Recovery: this is what a resuming Claude does FIRST after bootstrap. Format: one imperative action + acceptance check. Lifecycle: volatile.

## Do this next

**Context:** The most recent discovery cycle ran a **deliberately UN-SEEDED** candidate (TASK-0023) — **comprehension & documentation of undocumented legacy industrial control-system logic (PLC/SCADA/DCS, original integrator gone)** for asset-heavy operators — and **killed it at the both-sides vacuum screen → 54 → ARCHIVE** (fourth Archived row). This candidate was **engineered to satisfy every ADR-017 criterion** (no court-enforceable dispute right, no headline $-stat, pain borne as specialist labor + outage/safety/OT-cyber risk, orphaned at the OT/IT seam, embarrassing) **— and it capped anyway.** Both wedges owned & productized: comprehension = **Copia AI/Copilot** ("understands legacy code," PLC doc-gen + summarization + Ladder↔ST translation; $16.4M Series A) + plccopilot + SI bench; capture = **Octoplant/AMDT** (per-device version control of code **and docs**) + Software Defined Automation + PLCdoc. This is the **7th consecutive cap (79/71/72/63/64/58/54)** vs the single 83 (OPP-001b) → **ADR-018**. Project 01 remains **VALIDATION HOLD, operator-gated** (TASK-0014). Still no second ≥80 project.

**Action (autonomous worker):** Leading project still operator-gated, no other ≥80 → **do NOT idle, do NOT re-desk-validate project 01.** **Continue NET-NEW discovery (runtime §5 / priority 3)**, but apply the **ADR-018 doctrine (load-bearing — it SUPERSEDES the candidate-selection guidance of ADR-015/017), in order, BEFORE deep research:**

1. **APPLY THE STRIP-OUT-THE-AI SCREEN FIRST — before the vacuum check.** ADR-018's core finding: OPP-007 was built specifically to remove the visible money pool (ADR-015) and the dispute/services double-magnet (ADR-017) and it **still capped**, because the real failure mode is the **archetype**: all 7 scored picks were "use AI to read/assemble/comprehend/document an artifact corpus → a defensible record/understanding," which is commoditized by horizontal AI AND absorbed for ~free by whoever owns the artifact's repository (Copia owns the code repo; Procore the project record; Octoplant the backups). **Ask: "If I strip out the AI, is there still a business — a workflow I own, a transaction I sit in, data only I have, or a channel only I reach?"** If the answer is "no, the product IS the AI reading documents," **REJECT the candidate before spending a cycle.**
2. **The next candidate must LEAD with an OWNED position** — a proprietary workflow, a transaction/network position, a proprietary data asset, or a distribution channel — where **AI is the tool, not the product** (back to MISSION's first principle). This is the only profile the 0/7 record has not already falsified.
3. **ADR-015 (visible-money-pool) and ADR-017 (services-industry / dispute-magnet) are now SECOND-STAGE filters**, not the lead screen. They remain valid as descriptions of *why* the documentation-archetype picks capped, but removing those features alone does not produce a winner (OPP-007 proved it).
4. **STILL run the universal both-sides named-incumbent vacuum check** (name prevention/capture-side and recovery/comprehension-side incumbents; show the specific artifact none sell) before any deep research. Verify "no owner" affirmatively.

Find + score ONE Blue-Ocean B2B opportunity using `skills/opportunity-research/SKILL.md` + the ADR-008 rubric, with disprove discipline. **Run the strip-out-the-AI screen, THEN the vacuum screen, BEFORE deep research.** If ≥80 → create `projects/02-<slug>/` + validation artifacts; else log to the ledger (MONITOR/ARCHIVE) and record the meta-learning.

**Do NOT re-run:** IRA, PFAS, CBAM, EU AI Act, SEC-cyber, EU Pay Transparency, ocean-freight D&D, construction delay/change-order claims, **legacy PLC/SCADA/DCS comprehension** (all scored). Do NOT pick another documentation / reconstruction / comprehension artifact (that is the archetype failure mode). Do NOT re-desk-validate project 01 (operator-gated).

**Operator actions outstanding (do NOT auto-do):**
1. **TASK-0014** — primary discovery for project 01: 5–10 buyer/firm interviews (WTP + compete-vs-channel) + FOIA/TIGTA/§6418 data (real audit cadence). The ONLY thing that unblocks project 01 → BUSINESS_MODEL.md.
2. **TASK-0002** — enable unattended systemd loop (`automation/install.sh`) — operator go-ahead required.
3. **TASK-0021** — cycles must run as `tar` (not `sudo`) for git/file writes, or the supervisor must `chown` the repo to `tar` on start, to stop intermittent `.git`/queue permission failures. (No recurrence this cycle — `queue/active` was tar-owned — but the root cause persists.)
4. Rotate Discord token (historically pasted in chat).

**Acceptance check:** Next cycle produces a new scored opportunity record in `research/` + a row in `research/OPPORTUNITIES.md` + RESEARCH.md (and a `projects/02-*/` if ≥80); state files refreshed; Discord posted; committed. The candidate must **pass the ADR-018 strip-out-the-AI screen FIRST** (must own a workflow / transaction / data / channel — must NOT be "AI reads/documents an artifact"), then pass an affirmative both-sides named-incumbent vacuum check before any deep research. Do not re-validate project 01 and do not re-score any already-scored sector.

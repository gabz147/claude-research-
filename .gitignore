# Secrets — NEVER commit
config/.env
*.env
*.token
*.secret

# Runtime
logs/*.log
logs/*.jsonl
*.lock
state/*.lock

# OS / editor
.DS_Store
*.swp

# Stray external symlinks (not part of repo memory)
cr-bot
